import React from 'react';
import styles from './css/main_var.module.css';

const MainVar = (props) => (
            <>
            <div className={styles.mainvar}>
                Catube
            </div>

            <div className={styles.sidevar}>
                hello
            </div>
            </>
    );

export default MainVar;